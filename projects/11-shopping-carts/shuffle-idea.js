const ar = [iconEl, iconEl, iconEl ......... iconEl] // say 20

const ar2 = [ 0, 1, .... 19]

ar.forEach(iconEl => {
    // randomly get idx of ar2
    // iconEl.style.order = ar2[idx]
    // cut the number with idx from ar2 by splice()
})  